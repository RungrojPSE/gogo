# Initial setup

```bash
# new repo in Github then use ropo name as this
go mod init github.com/RungrojPSE/gogo

# initial
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/RungrojPSE/gogo.git
git push -u origin main
git tag "v1.0.0"
git push --tags

# update
git add .
git commit -m "update something"
git push

# how to do next
```
